# VeoVision - Football AI Dependencies

# Core ML/CV Framework
ultralytics>=8.3.0      # YOLOv8 object detection framework
torch>=2.4.0           # PyTorch (CPU version)
torchvision>=0.19.0    # PyTorch vision utilities

# Roboflow & Inference
roboflow>=1.1.0        # Dataset download and management
# YOLO26 / newer Roboflow hosted weights require a recent inference client (see yolo26n backend).
inference>=1.2.7      # Roboflow inference library for model inference
# inference-gpu>=0.60.0  # GPU version (uncomment if using GPU)

# Computer Vision & Video Processing
supervision>=0.23      # Video processing, detection annotation, and tracking
opencv-python>=4.10    # Computer vision library
pillow>=10.0.0        # Image manipulation

# Scientific Computing
numpy>=2.3,<2.4        # Numerical computing (version constraint for compatibility)
pandas>=2.0.0         # Data manipulation
scipy>=1.14           # Scientific computing

# Machine Learning & Clustering
# transformers 5.x can error on torch 2.4.x CPU when importing SigLIP (TeamClassifier).
transformers>=4.56,<5.0   # Hugging Face transformers (for SigLIP model)
umap-learn>=0.5.0     # UMAP for dimensionality reduction
scikit-learn>=1.3.0   # Machine learning utilities (KMeans clustering)

# Visualization
matplotlib>=3.7.0     # Plotting and visualization
seaborn>=0.12.0       # Statistical visualization
plotly>=5.0.0         # Interactive 3D visualization

# Jupyter Notebook Support
jupyter>=1.0.0        # Jupyter notebook
ipython>=8.12.0       # Interactive Python shell
ipywidgets>=8.1       # Interactive widgets for notebooks

# Utilities
pyyaml>=6.0           # YAML file parsing
tqdm>=4.66            # Progress bars
requests>=2.31.0      # HTTP library
more-itertools>=10.0.0 # Additional iterator tools (chunked function)
gdown>=4.7.0          # Google Drive download utility

# Sports Repository (installed via git)
# git+https://github.com/roboflow/sports.git  # Team classification utilities

# Optional: For GPU support (uncomment if using NVIDIA GPU)
# torch>=2.4.0+cu118   # PyTorch with CUDA 11.8
# torchvision>=0.19.0+cu118

# Optional: For AMD GPU support (experimental)
# torch-directml>=0.2.0
